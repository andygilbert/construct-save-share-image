"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.ImageSaveShareSMS;
	
	PLUGIN_CLASS.Type = class imageSaveShareSMSType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}